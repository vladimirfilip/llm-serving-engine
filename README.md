# llm-serving-engine

A single-GPU LLM serving engine: continuous batching, paged KV cache, and Triton kernels on
the hot path, measured against vLLM, SGLang and TensorRT-LLM with `python -m bench`.

<!-- BENCH:START -->
<!-- BENCH:END -->
